@model IEnumerable<LibSpace_Aspnet.Models.PreRequisitum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Idleitor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Idlivro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoLevantamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Idleitor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Idlivro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoLevantamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idreserva">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idreserva">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idreserva">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
